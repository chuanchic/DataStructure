package com.test.datastructure.trie;

public class Note {
    /**
     * 什么是 Trie(字典树、前缀树) ？
     *   从根节点开始，单词的每个字符作为一个节点，直到叶子节点
     *   每个节点有n个指向下个节点的指针
     *   class Node{
     *       char c;
     *       Map<char, Node> next;
     *       boolean isWord;
     *   }
     *
     *   查询每个条目的时间复杂度，和条目多少无关
     *   时间复杂度为 O(W)，W 为查询单词的长度
     *
     *
     *
     *
     */
}
